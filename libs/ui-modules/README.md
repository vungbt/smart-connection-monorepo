# ui-modules

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ui-modules` to build the library.

## Running unit tests

Run `nx test ui-modules` to execute the unit tests via [Vitest](https://vitest.dev/).
